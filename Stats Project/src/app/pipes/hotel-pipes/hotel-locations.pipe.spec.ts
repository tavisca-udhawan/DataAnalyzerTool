import { HotelLocationsPipe } from './hotel-locations.pipe';

describe('HotelLocationsPipe', () => {
  it('create an instance', () => {
    const pipe = new HotelLocationsPipe();
    expect(pipe).toBeTruthy();
  });
});
