import { FlightDestinationPipe } from './flight-destination.pipe';

describe('FlightDestinationPipe', () => {
  it('create an instance', () => {
    const pipe = new FlightDestinationPipe();
    expect(pipe).toBeTruthy();
  });
});
