import { FlightSourcePipe } from './flight-source.pipe';

describe('FlightSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new FlightSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
